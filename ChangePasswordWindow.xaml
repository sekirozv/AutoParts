<Window x:Class="GardensRu.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена пароля" 
        Height="300" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFE1F5FE">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Times New Roman"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Times New Roman"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Times New Roman"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Смена пароля пользователя" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"
                   HorizontalAlignment="Center" Foreground="#01579B"/>

        <TextBlock Grid.Row="1" Text="Новый пароль:" Margin="5"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="1" Margin="5,25,5,5"/>

        <TextBlock Grid.Row="2" Text="Подтверждение пароля:" Margin="5"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Margin="5,25,5,5"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Margin="5" 
                    Background="#FFE57373" BorderBrush="#FFEF5350" Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Сохранить" Width="80" Margin="5" 
                    Background="#FF81C784" BorderBrush="#FF66BB6A" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>